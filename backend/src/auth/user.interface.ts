export interface User {
  githubId: string;
  username: string;
  avatarUrl: string;
  accessToken: string;
  playerId: number;
}

export interface UserInfo {
  githubId: string;
  username: string;
  avatarUrl: string;
  playerId: number;
}
