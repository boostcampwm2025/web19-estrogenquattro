export interface User {
  githubId: string;
  username: string;
  avatarUrl: string;
  accessToken: string;
}

export interface UserInfo {
  githubId: string;
  username: string;
  avatarUrl: string;
}
