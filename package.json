{
  "name": "web19-estrogenquattro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && pnpm install && cd ../frontend && pnpm install",
    "build:backend": "cd backend && pnpm build",
    "build:frontend": "cd frontend && pnpm build",
    "build:all": "pnpm build:backend && pnpm build:frontend",
    "deploy": "pnpm install:all && pnpm build:all && pm2 start ecosystem.config.js",
    "deploy:reload": "git pull && pnpm install:all && pnpm build:all && pm2 delete all && pm2 start ecosystem.config.js",
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop ecosystem.config.js",
    "restart": "pm2 reload ecosystem.config.js",
    "logs": "pm2 logs",

    "dev": "concurrently \"pnpm dev:client\" \"pnpm dev:server\"",
    "dev:client": "cd client && pnpm dev",
    "dev:server": "cd server && pnpm dev",
    "build": "pnpm build:client && pnpm build:server",
    "build:client": "cd client && pnpm build",
    "build:server": "cd server && pnpm build",
    "start:prod": "NODE_ENV=production node server/dist/index.js",
    "typecheck": "cd client && pnpm typecheck && cd ../server && pnpm typecheck",

    "install:new": "cd client && pnpm install && cd ../server && pnpm install",
    "deploy:new": "pnpm install:new && pnpm build && pm2 start ecosystem.new.config.cjs",
    "deploy:new:reload": "git pull && pnpm install:new && pnpm build && pm2 delete mogakco-single && pm2 start ecosystem.new.config.cjs"
  }
}
